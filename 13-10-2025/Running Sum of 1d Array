class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {

        int prefixsum=0;

        for(int i=0;i<nums.size();i++)
        {
            prefixsum=prefixsum+nums[i];

            nums[i]=prefixsum;
        }

        return nums;
        
    }
};
